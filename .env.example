# 팀 공유용 환경변수 템플릿 — 실제 값은 .env 에 넣는다 (.env는 커밋 금지).
# 복사: cp .env.example .env

# 백엔드 API 베이스 URL
VITE_API_BASE_URL=change-me-to-your-backend-api-base-url

# 로그인 세션 우회용 — true면 카페 추천 API 대신 mock 데이터 사용.
# 로그인 기능이 붙으면 false로 바꾸면 실제 API로 전환됨.
VITE_USE_MOCK_CAFES=false
